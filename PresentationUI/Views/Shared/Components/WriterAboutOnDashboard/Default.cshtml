@using EntityLayer.Concrete
@model List<Writer>

<div class="row">
    <div class="col-12 grid-margin stretch-card">
        <div class="card table-responsive-sm">
            <div class="card-body">
                <h4 class="card-title">Yazar Hakkında</h4>
                @foreach (var item in Model)
                {
                    <div class="d-flex">
                        <div class="d-flex align-items-center mr-4 text-muted font-weight-light">
                            <i class="mdi mdi-account-outline icon-sm mr-2"></i>
                            <span>@item.WriterName</span>
                        </div>

                        <div class="d-flex align-items-center text-muted font-weight-light">
                            <i class="mdi mdi-mail icon-sm mr-2"></i>
                            <span>@item.WriterMail</span>
                        </div>
                    </div>

                    <div class="d-flex mt-5 align-items-top">
                        @if (ViewBag.writerImage != null)
                        {
                            <img src="~/WriterImageFiles/@item.WriterImage" class="img-sm rounded-circle mr-3" alt="image">
                        }
                        else
                        {
                            <img src="~/WriterImageFiles/user-icon.jpg" class="img-sm rounded-circle mr-3" alt="image">
                        }

                        <div class="mb-0 flex-grow">
                            <h5 class="mr-2 mb-2">@item.WriterName</h5>
                            <p class="mb-0 font-weight-light">@item.WriterAbout</p>
                        </div>

                        <div class="ml-auto">
                            <i class="mdi mdi-heart-outline text-muted"></i>
                        </div>

                    </div>
                }
            </div>
        </div>
    </div>
</div>
